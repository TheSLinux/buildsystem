# Maintainer: Duc M. Tran <901stt@gmail.com>
# Original Contributor: damir <damir@archlinux.org>
# Original Committer: Manolis Tzanidakis <manolis@archlinux.org>
# Original Maintainer: Gaetan Bisson <bisson@archlinux.org>

pkgdesc='Command line interface to the X11 clipboard'
url='http://sourceforge.net/projects/xclip'
license=('GPL')
depends=('libxmu')
arch=('i686' 'x86_64')
source=("http://downloads.sourceforge.net/sourceforge/xclip/${pkgbase}-${pkgver}.tar.gz")
sha1sums=('aed2cff18b9aecfe3377ad064c6a67518bbec211')

build() {
	cd "${srcdir}/${pkgbase}-${pkgver}"
	./configure --prefix=/usr
	make
}

package() {
	cd "${srcdir}/${pkgbase}-${pkgver}"
	make DESTDIR="${pkgdir}" install
}

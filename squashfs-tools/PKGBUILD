# Maintainer: Anh K. Huynh <kyanh@theslinux.org>
# Original Maintainer: Sergej Pupykin <pupykin.s+arch@gmail.com>
# Original Maintainer: Aaron Griffin <aaron@archlinux.org>
# Original Original TU: Jeff Mickey <j@codemac.net>
# Original Contributor: ciccio.a
pkgdesc="Tools for squashfs, a highly compressed read-only filesystem for Linux."
url="http://squashfs.sourceforge.net"
license=("GPL")
arch=('i686' 'x86_64')
depends=('zlib' 'lzo2' 'xz')
source=("http://downloads.sourceforge.net/sourceforge/squashfs/squashfs${pkgver}.tar.gz")

build() {
    cd ${srcdir}/squashfs${pkgver}/${pkgname}
    make XZ_SUPPORT=1 LZO_SUPPORT=1 LZMA_XZ_SUPPORT=1
}

package() {
    cd ${srcdir}/squashfs${pkgver}/${pkgname}
    install -Dm755 mksquashfs ${pkgdir}/usr/bin/mksquashfs
    install -m755 unsquashfs ${pkgdir}/usr/bin/unsquashfs
}

# Maintainer: Severus <severus@theslinux.org>
# Origin Maitainer: Alexander Rødseth <rodseth@gmail.com>
# Origin Contributor: Judd Vinet <jvinet@zeroflux.org>

pkgdesc="Client for battery, power, and thermal readings"
arch=('x86_64' 'i686')
url="http://sourceforge.net/projects/acpiclient/"
license=('GPL2')
depends=('glibc')
source=("http://downloads.sourceforge.net/acpiclient/$pkgbase-$pkgver.tar.gz")

build() {
  cd "$srcdir/$pkgbase-$pkgver"

  ./configure --prefix=/usr
  make
}

package() {
  cd "$srcdir/$pkgbase-$pkgver"

  make DESTDIR"=$pkgdir" install
}

# vim:set ts=2 sw=2 et:

# Maintainer: Severus <severus@theslinux.org>
# Original Maitainer: Dave Reisner <dreisner@archlinux.org>
# Original Contributor: Daniel J Griffiths <ghost1227@archlinux.us>
# Original Contributor: Jeff Mickey <jeff@archlinux.org>
# Original Contributor: Alexander Fehr <pizzapunk gmail com>

pkgdesc='C library that performs DNS requests and name resolves asynchronously'
arch=('i686' 'x86_64')
url='http://c-ares.haxx.se/'
license=('custom')
depends=('glibc')
options=('!libtool')
source=("http://c-ares.haxx.se/download/$pkgbase-$pkgver.tar.gz"{,.asc}
        'LICENSE')

build() {
  cd "$pkgbase-$pkgver"

  ./configure --prefix=/usr --enable-shared
  make
}

package() {
  cd "$pkgbase-$pkgver"

  make DESTDIR="$pkgdir" install
  install -Dm644 "$srcdir/LICENSE" "$pkgdir/usr/share/licenses/$pkgbase/LICENSE"
}

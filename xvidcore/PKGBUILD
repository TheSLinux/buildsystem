# Maintainer: Severus <severus@theslinux.org>
# Origin Maitainer: tobias <tobias@archlinux.org>
# Origin Contributor: Sarah Hay <sarah@archlinux.org>

pkgdesc="XviD is an open source MPEG-4 video codec"
arch=('i686' 'x86_64')
url="http://www.xvid.org/"
license=('GPL')
depends=('glibc')
makedepends=('nasm')
source=(http://downloads.xvid.org/downloads/${pkgbase}-${pkgver}.tar.bz2)

build() {
  cd "${srcdir}/${pkgbase}/build/generic"
  ./configure --prefix=/usr
  make
}

package() {
  cd "${srcdir}/${pkgbase}/build/generic"
  make DESTDIR="${pkgdir}" install

  #Fix dynamic libraries
  cd "${pkgdir}/usr/lib"
  _mylib=$(basename libxvidcore.so.*)
  ln -sf ${_mylib} libxvidcore.so.4
  ln -sf ${_mylib} libxvidcore.so
}
